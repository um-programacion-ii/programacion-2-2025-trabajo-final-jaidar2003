###############################################
# CONFIGURACIÓN TF25 - CÁTEDRA (LOGIN AUTOMÁTICO)
# Copiar este archivo como ".env" en la raíz del repo
# y completar los valores reales. Docker Compose lo
# tomará automáticamente.
###############################################

# URL base del servidor de la cátedra
TF25_CATEDRA_BASE_URL=http://192.168.194.250:8080

# Endpoint real de autenticación (JHipster style)
# En la cátedra suele ser /api/authenticate o /api/v1/authenticate
TF25_CATEDRA_AUTH_LOGIN_PATH=/api/authenticate

# Credenciales de tu usuario en la cátedra
TF25_CATEDRA_AUTH_USERNAME=<TU_USUARIO>
TF25_CATEDRA_AUTH_PASSWORD=<TU_PASSWORD>

###############################################
# OPCIONAL: TOKEN ESTÁTICO (si lo definís, el proxy
# NO hará login y usará este JWT directamente). Podés usar cualquiera de estas vars:
# - TF25_CATEDRA_TOKEN
# - TF25_CATEDRA_BEARER_TOKEN
###############################################
# TF25_CATEDRA_TOKEN=
# TF25_CATEDRA_BEARER_TOKEN=

# Alternativa: indicar un archivo que contenga el JWT (una sola línea)
# Si está definido, el proxy usará ese token y NO hará login.
# Por defecto el proxy intentará leer ../token.txt (raíz del repo) al correr desde el módulo proxy
# TF25_CATEDRA_BEARER_TOKEN_FILE=../token.txt
